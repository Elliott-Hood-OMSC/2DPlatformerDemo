using UnityEngine;

public class TransformSquisher2D : MonoBehaviour
{
    /*[SerializeField] private TemporaryEffector defaultSquishEffector;
    public AdditiveEffectorValue squishValue = new AdditiveEffectorValue();

    [Button]
    public void Squish(TemporaryEffector temporaryEffector = null)
    {
        squishValue.AddTemporaryEffector(temporaryEffector);
    }

    private void Update()
    {
        float value = Mathf.Clamp(squishValue.Evaluate(), -0.8f, 0.8f);
        transform.localScale = new Vector3(1 + value, 1 - value, 1);
    }*/
}
